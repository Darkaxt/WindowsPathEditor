<Window x:Class="WindowsPathEditor.ScanningWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:my="clr-namespace:WindowsPathEditor"
        Title="Automatically add bin directories"
        Height="300" Width="520"
        ResizeMode="NoResize"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Background="{StaticResource WindowBackgroundBrush}"
        WindowStartupLocation="CenterScreen"
        Icon="/WindowsPathEditor;component/Resources/view_text.ico">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title / status -->
        <Label Grid.Row="0"
               x:Name="whatLabel"
               Content="Searching. Please wait..."
               FontSize="{StaticResource FontSizeTitle}"
               FontWeight="SemiBold"
               Margin="0,0,0,8" />

        <!-- Progress bar + current dir + Cancel -->
        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <ProgressBar x:Name="progressBar1" IsIndeterminate="True" Margin="0,0,0,4" />
                <Label x:Name="currentDirectory"
                       Content=""
                       FontSize="{StaticResource FontSizeSmall}"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       ClipToBounds="True"
                       Height="20" />
            </StackPanel>
            <Button Grid.Column="1"
                    x:Name="cancelButton"
                    Content="Cancel"
                    Width="80"
                    Margin="8,0,0,0"
                    VerticalAlignment="Top"
                    Click="Cancel_Click" />
        </Grid>

        <!-- Results list -->
        <ListBox Grid.Row="2"
                 ItemsSource="{Binding Paths}"
                 IsTabStop="False"
                 Margin="0,0,0,8">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding Path}" IsChecked="{Binding IsSelected}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Add button -->
        <Button Grid.Row="3"
                x:Name="addButton"
                Content="Add"
                Width="80"
                HorizontalAlignment="Right"
                IsEnabled="False"
                Click="addButton_Click" />
    </Grid>
</Window>
